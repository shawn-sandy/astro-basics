---
import { contentfulClient } from "#libs/contentful";
import type { Articles } from "#libs/contentful";

export async function getStaticPaths() {
  const entries = await contentfulClient.getEntries<Articles>({
    content_type: "blogPost",
  });
}
---
