---
title: Installation
description: Step-by-step installation guide for astro-basics.
---

# Installation Guide

This guide covers different ways to install and set up astro-basics depending on your use case.

## System Requirements

- **Node.js**: Version 18 or higher
- **npm**: Version 8 or higher (or yarn/pnpm equivalent)
- **Git**: For cloning the repository

## Installation Options

### Full Project Setup

For complete project with all features, examples, and development tools:

```bash
# Clone the repository
git clone https://github.com/shawn-sandy/astro-basics.git

# Navigate to project directory
cd astro-basics

# Install dependencies (takes ~4 minutes)
npm install

# Set up environment
cp .env.example .env

# Install pre-commit hooks
npm run prepare

# Install Playwright browsers (for E2E tests)
npx playwright install
```

### Component Library Only

To use astro-basics components in an existing Astro project:

```bash
npm install astro-basics-website
```

## Post-Installation Setup

### 1. Environment Configuration

Edit your `.env` file with the required keys:

```env
# Authentication (Required)
PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_your_key_here
CLERK_SECRET_KEY=sk_test_your_key_here

# Site URL
SITE_URL=http://localhost:4321

# Database (Optional)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Turso (Optional)
TURSO_DATABASE_URL=libsql://your-database.turso.io
TURSO_AUTH_TOKEN=your_auth_token_here
```

### 2. Database Setup (Optional)

If using database features:

```bash
# Initialize database
npm run db:setup

# Check connection
npm run db:check

# Run migrations
npm run db:migrate
```

### 3. Verify Installation

Start the development server:

```bash
npm run start
```

Visit `http://localhost:4321` to see your site running.

## Development Tools

### Code Quality

```bash
# Run all linting and formatting
npm run fix:all

# Type checking
npm run type-check

# Individual tools
npm run lint          # ESLint
npm run lint:styles   # StyleLint
npm run format        # Prettier
```

### Testing

```bash
# Unit tests
npm test

# E2E tests
npm run test:e2e

# Test reports
npm run test:e2e:report
```

### Build and Deploy

```bash
# Production build
npm run build

# Preview build
npm run preview

# Deploy to Netlify
npm run deploy:prod
```

## Component Usage

### In Astro Files

```astro
---
import { Header, Footer } from 'astro-basics-website'
---

<Header title="My Site" />
<main>
  <!-- Your content -->
</main>
<Footer />
```

### With TypeScript

```typescript
import type { Props } from 'astro-basics-website'

// Use component props with full type safety
```

## Troubleshooting

### Common Issues

**1. Installation takes too long**

- Expected: npm install takes ~4 minutes
- Warnings during installation are normal

**2. Authentication errors**

- Ensure `PUBLIC_CLERK_PUBLISHABLE_KEY` and `CLERK_SECRET_KEY` are set
- Use dummy keys for build-only testing

**3. E2E tests failing**

- Run `npx playwright install` to install browsers
- Ensure dev server is running on port 4321

**4. Build warnings**

- getStaticPaths warnings in dynamic pages are expected
- These don't affect functionality

### Getting Help

- Check the [GitHub Issues](https://github.com/shawn-sandy/astro-basics/issues)
- Review the [troubleshooting section](/guide/troubleshooting/) (coming soon)
- Ensure you're using the latest version

## Next Steps

- **[Configuration](/guide/getting-started/configuration/)** - Configure your project
- **[Components](/guide/components/)** - Explore available components
- **[Development](/guide/getting-started/development/)** - Development workflow
