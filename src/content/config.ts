// Deprecated: superseded by src/content.config.ts (Astro content-layer API).
// Astro only loads collections from the root content.config.ts; this legacy
// path is never read. Left in place as an inert marker rather than removed
// via `rm`, which is blocked by this session's permissions.
export const collections = {}
